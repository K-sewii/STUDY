#include <bits/stdc++.h>
using namespace std;



int main(){ 
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);

    ifstream in;
    ofstream out;
    out.open("square.txt", ios::app);


    in.close();
    out.close();
    return 0;
}